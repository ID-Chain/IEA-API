/**
 * IDChain Agent REST API
 * Credential Controllers
 */
'use strict';

module.exports = {
    offer: require('./offer'),
    request: require('./request'),
    credential: require('./credential')
};
